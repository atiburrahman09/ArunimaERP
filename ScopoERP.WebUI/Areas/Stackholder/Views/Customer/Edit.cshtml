@model ScopoERP.Stackholder.ViewModel.CustomerViewModel

@{
    ViewBag.Title = "Update Customer";
}

<div class="col-sm-offset-3 col-sm-6">
    <section class="panel">
        <header class="panel-heading">
            Update Customer
        </header>
        <div class="panel-body">

            @using (Html.BeginForm("Edit", "Customer", FormMethod.Post, new { @class = "form-horizontal" }))
            {
                @Html.ValidationSummary(true)

                @Html.HiddenFor(model => model.CustomerID)

                <div class="form-group">
                    <label class="col-lg-4 col-sm-4 control-label">Customer Name</label>
                    <div class="col-lg-8">
                        @Html.TextBoxFor(model => model.CustomerName, new { @class = "form-control" })
                        @Html.ValidationMessageFor(model => model.CustomerName)
                    </div>
                </div>

                <div class="form-group">
                    <div class="col-lg-offset-4 col-lg-4">
                        @Html.ActionLink("Cancel", "Index", "Customer", null, new { @class = "btn btn-block btn-default" })
                    </div>
                    <div class="col-lg-4">
                        <input type="submit" class="btn btn-block btn-primary" value="Update" />
                    </div>
                </div>
            }

        </div>
    </section>
</div>
