@model ScopoERP.Common.ViewModel.FactoryViewModel

@{
    ViewBag.Title = "Update Factory";
}

<div class="col-sm-offset-3 col-sm-6">
    <section class="panel">
        <header class="panel-heading">
            Update Factory
        </header>
        <div class="panel-body">

            @using (Html.BeginForm("Edit", "Factory", FormMethod.Post, new { @class = "form-horizontal" }))
            {
                @Html.ValidationSummary(true)

                @Html.HiddenFor(model => model.FactoryID)

                <div class="form-group">
                    <label class="col-lg-4 col-sm-4 control-label">Factory Name</label>
                    <div class="col-lg-8">
                        @Html.TextBoxFor(model => model.FactoryName, new { @class = "form-control" })
                        @Html.ValidationMessageFor(model => model.FactoryName)
                    </div>
                </div>

                <div class="form-group">
                    <div class="col-lg-offset-4 col-lg-4">
                        @Html.ActionLink("Cancel", "Index", "Factory", null, new { @class = "btn btn-block btn-default" })
                    </div>
                    <div class="col-lg-4">
                        <input type="submit" class="btn btn-block btn-primary" value="Update" />
                    </div>
                </div>
            }

        </div>
    </section>
</div>
